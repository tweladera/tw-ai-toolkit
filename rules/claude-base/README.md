# claude-base

Base behavioral rules for Claude Code when working in a repo that uses tw-ai-toolkit.

Defines context loading order, skill invocation conventions, and post-edit workflow.

**To use:** Copy the block from `fragment.md` into your consumer repo's `CLAUDE.md`.

See `rule.md` for full documentation.
