# cursor-base

Base behavioral rules for Cursor when working in a repo that uses tw-ai-toolkit.

**To use:** Copy the block from `fragment.md` into your consumer repo's `.cursorrules`.

See `rule.md` for full documentation.
