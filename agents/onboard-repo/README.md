# onboard-repo

Analyzes a repository and sets up tw-ai-toolkit with configuration tailored to the project.

**Invocation:** `/tw-onboard-repo`

Detects the tech stack, installs the toolkit, generates a tailored `config.json`,
and recommends MCP servers based on enterprise tools detected in the repo.

See `agent.md` for full documentation.
